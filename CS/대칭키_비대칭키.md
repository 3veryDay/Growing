# **대칭키 vs 비대칭키**

대칭키는 암호화와 복호화에 사용되는 키가 같은 암호화 시스테이고, 비대칭키는 암호화와 복호화에 사용되는 키가 다른 암호화 시스템이다. 

대칭키부터 자세하게 보면, 이는 키가 같기 때문에, 누구든 키만 가지고 있으면 암호문을 복호화할 수 있다. 그렇기 때문에 Key Distribution problem이 발생한다. 이를 위해서 RSA기반 키 교환 방식이 나왔고, 이를 사용한게 TLS handshaking이다. 

비대칭키는, 개인키와 공개키 두개의 조합으로 이뤄진다. 공개키로 암호화를 하고 개인키로 복호화를 하는 방식은 기밀성을 위해서 하는 것이면 TLS RSA도 이 방식이다. 반대로 암호화하는 쪽에서 개인키를 갖고 암호화를 하고, 복호화를 할 때 공개키를 갖고 하면 인증, 전자 성명의 목적을 갖고 있다. JWT 성명이 여기서 나온다. 

SSL, TLS은 이 둘을 다 사용하는 hybrid 암호화 방식을 사용한다. 비대칭키 방식을 사용해서 대칭키에 이용할 키를 주고 받고, 이 개인키를 사용해서 HTTPS 암호화 터널이 완성되면 개인키로 빠르게 내용을 암호화, 복호화하며 주고 받는다. 

---

## 💡 핵심 개념 (Definition)
> 한 줄 요약: 대칭키는 기밀성, 비대칭키는 암호화를 공개키로 하면 기밀성, (SSL, TLS) 그리고 암호화를 비밀키로 하면 전자서명, 인증으로 사용된다. 

- 

## ⚙️ 동작 원리 / 구조 (Mechanism)
```java
// 필요 시 코드 예제 작성
```

## 🆚 장단점 / 비교 (Pros & Cons)
- 장점: 비대칭키는 느리지만, 안전하고
- 단점: 대칭키는 빠르지만, key distribution 문제 등이 발생한다. 
## 추가적으로 정리할 내용 
- HTTPS에서 SSL,TLS handshake
- Jwt token
- spring에서 Redis를사용해서 refresh token을 저장해두기
- access token과 refresh token이 작동하는 방식
- spring security에서 인증 인가를 담당하는 filter chain이 있고, 여기서 인증 객체를 담은 security context가 존재함. 
---

## 🔗 참고 자료 (References)
- [ ] 공식 문서 확인
- [ ] 블로그 정리 완료
