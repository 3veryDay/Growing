name: '🗓️ 오늘의 1시간 공부 주제 생성'

on:
  # 한국 시간(KST) 기준 월~금 자정에 실행 (UTC 15:00)
  schedule:
    - cron: '0 15 * * 1-5'
  # 수동 실행 버튼 (오늘 공부 더 하고 싶을 때 누르면 새로운 랜덤 주제 생성)
  workflow_dispatch:

jobs:
  create_daily_study:
    name: Create Daily Study Issue
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: 'Set KST Timezone'
        uses: szenius/set-timezone@v2.0
        with:
          timezoneLinux: 'Asia/Seoul'
            
      - name: 'Generate Random Issue Content'
        id: issue_content
        run: |
          # 📚 공부할 주제 배열
          TOPICS=(
            "Redis 활용" "SOP & CORS" "@Transactional 동작 원리" "B-Tree와 B+Tree (Index)" "JVM 메모리 구조 (Stack, Heap, Method)"
            "Spring MVC 구조" "인덱스 (Clustered vs Non-clustered)" "Java Stream API" "JWT" "대칭키 vs 비대칭키"
            "Spring Security 구조" "GC (Garbage Collection) 알고리즘" "DB Lock (Optimistic vs Pessimistic)" "WebFlux" "OAuth"
            "JPA Persistent Context (영속성 컨텍스트)" "멀티스레드와 스레드풀" "트랜잭션과 ACID" "IOC (Inversion of Control)" "NoSQL"
            "접근 제어자(Access Modifier)의 동작 방식" "Spring Data JDBC" "Replication" "Lambda Expression" "XSS"
            "QueryDSL" "Connection Pool" "Filter vs Interceptor" "String, hashCode(), equals()" "JOIN의 종류와 원리"
            "AOP (Aspect Oriented Programming)" "WebClient" "InnoDB 스토리지 엔진" "Optional 활용" "Flyway (DB 마이그레이션)"
            "MySQL의 실행계획" "Thread Safe 자료구조 (ConcurrentHashMap 등)" "Dynamic Proxy vs CGLIB" "Caching Strategy" "Generic"
            "Spring Bean 생명주기" "Covering Index" "Jigsaw (Java Module System)" "Map 구현체 비교 (HashMap, TreeMap, LinkedHashMap)" "정규화(Normalization)"
            "JUnit 테스트" "JCF (Java Collections Framework)" "Actuator" "Sequence" "Process vs Thread"
            "Web Polling (Long/Short)" "Comparable vs Comparator" "Spring Cloud" "static 키워드" "Trigger"
            "PSA (Portable Service Abstraction)" "Serialization (직렬화)" "Immutability (불변성)" "내장 Tomcat" "Java 버전별 특징 (8, 11, 17, 21)"
            "Auto Configure (자동 설정)" "Bulk Insert" "Functional Interface" "Java 예외 처리 계층 (Checked vs Unchecked)" "Logger (SLF4J/Logback)"
            "Runnable vs Callable" "Servlet" "Spring 예외처리 (ExceptionHandler)" "Cache Locality" "Profile (@Profile)"
            "Reflection API" "Server Sent Event (SSE)" "동기(Synchronous) vs 비동기(Asynchronous)" "기본키 생성 전략 (Identity/Sequence/Table)" "Jackson 라이브러리"
            "얕은 복사 vs 깊은 복사" "Annotation Processor" "Regular Expression (정규표현식)" "Build Tool (Gradle vs Maven)" "javac와 컴파일 과정"
            "null 처리" "Self Relationship" "Swagger (SpringDoc)" "final 키워드" "JMX (Java Management Extensions)"
            "Boxing/Unboxing" "volatile 키워드" "Java Record (Java 14+)" "인증(Authentication) vs 인가(Authorization)"
          )

          # 🎲 랜덤 인덱스 생성
          # 배열의 전체 길이($size)를 구하고, $RANDOM을 그 길이로 나눈 나머지 값을 인덱스로 사용
          SIZE=${#TOPICS[@]}
          RANDOM_INDEX=$(($RANDOM % $SIZE))
          TODAY_TOPIC=${TOPICS[$RANDOM_INDEX]}
          
          # 현재 날짜 및 시간 구하기 (제목에 시간까지 포함하면 중복 실행 시 구분이 더 쉬움, 여기선 날짜만 사용)
          CURRENT_DATE=$(date +'%Y-%m-%d')

          # 이슈 제목 & 본문 생성
          echo "ISSUE_TITLE=[$CURRENT_DATE] 오늘의 1시간 공부: $TODAY_TOPIC" >> "$GITHUB_ENV"
          {
            echo "ISSUE_BODY<<EOF"
            echo "## 🎯 오늘의 공부 주제"
            echo "# **$TODAY_TOPIC**"
            echo ""
            echo "---"
            echo ""
            echo "## 💡 핵심 개념 (Definition)"
            echo "> 한 줄 요약: "
            echo ""
            echo "- "
            echo ""
            echo "## ⚙️ 동작 원리 / 구조 (Mechanism)"
            echo "\`\`\`java"
            echo "// 필요 시 코드 예제 작성"
            echo "\`\`\`"
            echo ""
            echo "## 🆚 장단점 / 비교 (Pros & Cons)"
            echo "- 장점: "
            echo "- 단점: "
            echo "- 비교 대상: "
            echo ""
            echo "---"
            echo ""
            echo "## 🔗 참고 자료 (References)"
            echo "- [ ] 공식 문서 확인"
            echo "- [ ] 블로그 정리 완료"
            echo "EOF"
          } >> "$GITHUB_ENV"

      - name: 'Create GitHub Issue'
        uses: actions/github-script@v7
        env:
          TITLE: ${{ env.ISSUE_TITLE }}
          BODY: ${{ env.ISSUE_BODY }}
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: process.env.TITLE,
              body: process.env.BODY,
              labels: ['CS공부', 'TIL']
            })
